;  ==========================================================================
;   GENERATED by emit_gain.py -- DO NOT EDIT.
;
;   DemoVT v1.51 -- (C) 1992-94 VangeliSTeam (JCAB).
;
;   THE GAIN LADDER: 256 unrolled scale-and-add steps, one per volume
;   level, so that scaling a sample costs a call and an add rather
;   than a multiply. On an 8086 a multiply is over a hundred cycles
;   and this runs once per sample per channel, so the table is the
;   difference between playing and not.
;
;   THE PATCHER IS WHAT MAKES IT USABLE. Each cell ends in a jump
;   whose target is written at run time, so the mixer enters the
;   ladder at the cell for the volume it wants and leaves where it
;   was told to. The cells are the table AND the code.
;


PUBLIC  SetGains
SetGains:
; ---- G1: 16 cells --------------------------------------------

; gain 1
@@G1_00:
    DB 0C3h                                ; 0746  return -- NEAR, see the note above

; gain 15/16
@@G1_01:
    DB 66h                                     ; 0747  EBX := EBX shr 3 (signed)
    SAR BX,3
    DB 66h                                     ; 074b  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 074e  EAX := EAX - EBX
    DB 0C3h                                ; 0751  return -- NEAR, see the note above

; gain 7/8
@@G1_02:
    DB 66h                                     ; 0752  EBX := EBX shr 2 (signed)
    SAR BX,2
    DB 66h                                     ; 0756  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0759  EAX := EAX - EBX
    DB 0C3h                                ; 075c  return -- NEAR, see the note above

; gain 13/16
@@G1_03:
    DB 66h                                     ; 075d  EBX := EBX shr 2 (signed)
    SAR BX,2
    DB 66h                                     ; 0761  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0764  EAX := EAX - EBX
    DB 66h                                     ; 0767  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 076a  EAX := EAX - EBX
    DB 0C3h                                ; 076d  return -- NEAR, see the note above

; gain 3/4
@@G1_04:
    DB 66h                                     ; 076e  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 0771  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0774  EAX := EAX - EBX
    DB 0C3h                                ; 0777  return -- NEAR, see the note above

; gain 11/16
@@G1_05:
    DB 66h                                     ; 0778  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 077b  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 077e  EAX := EAX - EBX
    DB 66h                                     ; 0781  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 0784  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0787  EAX := EAX - EBX
    DB 0C3h                                ; 078a  return -- NEAR, see the note above

; gain 5/8
@@G1_06:
    DB 66h                                     ; 078b  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 078e  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0791  EAX := EAX - EBX
    DB 66h                                     ; 0794  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0797  EAX := EAX - EBX
    DB 0C3h                                ; 079a  return -- NEAR, see the note above

; gain 9/16
@@G1_07:
    DB 66h                                     ; 079b  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 079e  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07a1  EAX := EAX - EBX
    DB 66h                                     ; 07a4  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07a7  EAX := EAX - EBX
    DB 66h                                     ; 07aa  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07ad  EAX := EAX - EBX
    DB 0C3h                                ; 07b0  return -- NEAR, see the note above

; gain 1/2
@@G1_08:
    DB 66h                                     ; 07b1  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07b4  EAX := EAX - EBX
    DB 0C3h                                ; 07b7  return -- NEAR, see the note above

; gain 7/16
@@G1_09:
    DB 66h                                     ; 07b8  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07bb  EAX := EAX - EBX
    DB 66h                                     ; 07be  EBX := EBX shr 2 (signed)
    SAR BX,2
    DB 66h                                     ; 07c2  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07c5  EAX := EAX - EBX
    DB 0C3h                                ; 07c8  return -- NEAR, see the note above

; gain 3/8
@@G1_10:
    DB 66h                                     ; 07c9  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07cc  EAX := EAX - EBX
    DB 66h                                     ; 07cf  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 07d2  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07d5  EAX := EAX - EBX
    DB 0C3h                                ; 07d8  return -- NEAR, see the note above

; gain 5/16
@@G1_11:
    DB 66h                                     ; 07d9  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07dc  EAX := EAX - EBX
    DB 66h                                     ; 07df  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 07e2  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07e5  EAX := EAX - EBX
    DB 66h                                     ; 07e8  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07eb  EAX := EAX - EBX
    DB 0C3h                                ; 07ee  return -- NEAR, see the note above

; gain 1/4
@@G1_12:
    DB 66h                                     ; 07ef  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07f2  EAX := EAX - EBX
    DB 66h                                     ; 07f5  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07f8  EAX := EAX - EBX
    DB 0C3h                                ; 07fb  return -- NEAR, see the note above

; gain 3/16
@@G1_13:
    DB 66h                                     ; 07fc  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 07ff  EAX := EAX - EBX
    DB 66h                                     ; 0802  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0805  EAX := EAX - EBX
    DB 66h                                     ; 0808  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 080b  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 080e  EAX := EAX - EBX
    DB 0C3h                                ; 0811  return -- NEAR, see the note above

; gain 1/8
@@G1_14:
    DB 66h                                     ; 0812  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0815  EAX := EAX - EBX
    DB 66h                                     ; 0818  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 081b  EAX := EAX - EBX
    DB 66h                                     ; 081e  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0821  EAX := EAX - EBX
    DB 0C3h                                ; 0824  return -- NEAR, see the note above

; gain 1/16
@@G1_15:
    DB 66h                                     ; 0825  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0828  EAX := EAX - EBX
    DB 66h                                     ; 082b  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 082e  EAX := EAX - EBX
    DB 66h                                     ; 0831  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 0834  EAX := EAX - EBX
    DB 66h                                     ; 0837  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 2Bh, 0C3h                      ; 083a  EAX := EAX - EBX
    DB 0C3h                                ; 083d  return -- NEAR, see the note above

; the family's pointer table -- SetGains indexes it by volume
@@G1Table:
    DW OFFSET @@G1_00
    DW OFFSET @@G1_01
    DW OFFSET @@G1_02
    DW OFFSET @@G1_03
    DW OFFSET @@G1_04
    DW OFFSET @@G1_05
    DW OFFSET @@G1_06
    DW OFFSET @@G1_07
    DW OFFSET @@G1_08
    DW OFFSET @@G1_09
    DW OFFSET @@G1_10
    DW OFFSET @@G1_11
    DW OFFSET @@G1_12
    DW OFFSET @@G1_13
    DW OFFSET @@G1_14
    DW OFFSET @@G1_15

; ---- G2: 16 cells --------------------------------------------

; gain 1
@@G2_00:
    DB 0C3h                                ; 085e  return -- NEAR, see the note above

; gain 17/16
@@G2_01:
    DB 66h                                     ; 085f  EBX := EBX shr 3 (signed)
    SAR BX,3
    DB 66h                                     ; 0863  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0866  EAX := EAX + EBX
    DB 0C3h                                ; 0869  return -- NEAR, see the note above

; gain 9/8
@@G2_02:
    DB 66h                                     ; 086a  EBX := EBX shr 2 (signed)
    SAR BX,2
    DB 66h                                     ; 086e  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0871  EAX := EAX + EBX
    DB 0C3h                                ; 0874  return -- NEAR, see the note above

; gain 19/16
@@G2_03:
    DB 66h                                     ; 0875  EBX := EBX shr 2 (signed)
    SAR BX,2
    DB 66h                                     ; 0879  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 087c  EAX := EAX + EBX
    DB 66h                                     ; 087f  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0882  EAX := EAX + EBX
    DB 0C3h                                ; 0885  return -- NEAR, see the note above

; gain 5/4
@@G2_04:
    DB 66h                                     ; 0886  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 0889  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 088c  EAX := EAX + EBX
    DB 0C3h                                ; 088f  return -- NEAR, see the note above

; gain 21/16
@@G2_05:
    DB 66h                                     ; 0890  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 0893  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0896  EAX := EAX + EBX
    DB 66h                                     ; 0899  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 089c  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 089f  EAX := EAX + EBX
    DB 0C3h                                ; 08a2  return -- NEAR, see the note above

; gain 11/8
@@G2_06:
    DB 66h                                     ; 08a3  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 08a6  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08a9  EAX := EAX + EBX
    DB 66h                                     ; 08ac  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08af  EAX := EAX + EBX
    DB 0C3h                                ; 08b2  return -- NEAR, see the note above

; gain 23/16
@@G2_07:
    DB 66h                                     ; 08b3  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 08b6  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08b9  EAX := EAX + EBX
    DB 66h                                     ; 08bc  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08bf  EAX := EAX + EBX
    DB 66h                                     ; 08c2  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08c5  EAX := EAX + EBX
    DB 0C3h                                ; 08c8  return -- NEAR, see the note above

; gain 3/2
@@G2_08:
    DB 66h                                     ; 08c9  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08cc  EAX := EAX + EBX
    DB 0C3h                                ; 08cf  return -- NEAR, see the note above

; gain 25/16
@@G2_09:
    DB 66h                                     ; 08d0  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08d3  EAX := EAX + EBX
    DB 66h                                     ; 08d6  EBX := EBX shr 2 (signed)
    SAR BX,2
    DB 66h                                     ; 08da  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08dd  EAX := EAX + EBX
    DB 0C3h                                ; 08e0  return -- NEAR, see the note above

; gain 13/8
@@G2_10:
    DB 66h                                     ; 08e1  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08e4  EAX := EAX + EBX
    DB 66h                                     ; 08e7  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 08ea  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08ed  EAX := EAX + EBX
    DB 0C3h                                ; 08f0  return -- NEAR, see the note above

; gain 27/16
@@G2_11:
    DB 66h                                     ; 08f1  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08f4  EAX := EAX + EBX
    DB 66h                                     ; 08f7  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 08fa  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 08fd  EAX := EAX + EBX
    DB 66h                                     ; 0900  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0903  EAX := EAX + EBX
    DB 0C3h                                ; 0906  return -- NEAR, see the note above

; gain 7/4
@@G2_12:
    DB 66h                                     ; 0907  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 090a  EAX := EAX + EBX
    DB 66h                                     ; 090d  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0910  EAX := EAX + EBX
    DB 0C3h                                ; 0913  return -- NEAR, see the note above

; gain 29/16
@@G2_13:
    DB 66h                                     ; 0914  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0917  EAX := EAX + EBX
    DB 66h                                     ; 091a  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 091d  EAX := EAX + EBX
    DB 66h                                     ; 0920  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h                                     ; 0923  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0926  EAX := EAX + EBX
    DB 0C3h                                ; 0929  return -- NEAR, see the note above

; gain 15/8
@@G2_14:
    DB 66h                                     ; 092a  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 092d  EAX := EAX + EBX
    DB 66h                                     ; 0930  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0933  EAX := EAX + EBX
    DB 66h                                     ; 0936  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0939  EAX := EAX + EBX
    DB 0C3h                                ; 093c  return -- NEAR, see the note above

; gain 31/16
@@G2_15:
    DB 66h                                     ; 093d  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0940  EAX := EAX + EBX
    DB 66h                                     ; 0943  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0946  EAX := EAX + EBX
    DB 66h                                     ; 0949  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 094c  EAX := EAX + EBX
    DB 66h                                     ; 094f  EBX := EBX shr 1 (signed)
    SAR BX,1
    DB 66h, 03h, 0C3h                      ; 0952  EAX := EAX + EBX
    DB 0C3h                                ; 0955  return -- NEAR, see the note above

; the family's pointer table -- SetGains indexes it by volume
@@G2Table:
    DW OFFSET @@G2_00
    DW OFFSET @@G2_01
    DW OFFSET @@G2_02
    DW OFFSET @@G2_03
    DW OFFSET @@G2_04
    DW OFFSET @@G2_05
    DW OFFSET @@G2_06
    DW OFFSET @@G2_07
    DW OFFSET @@G2_08
    DW OFFSET @@G2_09
    DW OFFSET @@G2_10
    DW OFFSET @@G2_11
    DW OFFSET @@G2_12
    DW OFFSET @@G2_13
    DW OFFSET @@G2_14
    DW OFFSET @@G2_15

; ---- G3: 16 cells --------------------------------------------

; gain 1
@@G3_00:
    DB 0C3h                                ; 0976  return -- NEAR, see the note above

; gain 9
@@G3_01:
    DB 66h                                     ; 0977  EBX := EBX shl 3
    SHL BX,3
    DB 66h, 03h, 0C3h                      ; 097b  EAX := EAX + EBX
    DB 0C3h                                ; 097e  return -- NEAR, see the note above

; gain 5
@@G3_02:
    DB 66h                                     ; 097f  EBX := EBX shl 2
    SHL BX,2
    DB 66h, 03h, 0C3h                      ; 0983  EAX := EAX + EBX
    DB 0C3h                                ; 0986  return -- NEAR, see the note above

; gain 13
@@G3_03:
    DB 66h                                     ; 0987  EBX := EBX shl 2
    SHL BX,2
    DB 66h, 03h, 0C3h                      ; 098b  EAX := EAX + EBX
    DB 66h                                     ; 098e  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0991  EAX := EAX + EBX
    DB 0C3h                                ; 0994  return -- NEAR, see the note above

; gain 3
@@G3_04:
    DB 66h                                     ; 0995  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0998  EAX := EAX + EBX
    DB 0C3h                                ; 099b  return -- NEAR, see the note above

; gain 11
@@G3_05:
    DB 66h                                     ; 099c  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 099f  EAX := EAX + EBX
    DB 66h                                     ; 09a2  EBX := EBX shl 1
    SHL BX,1
    DB 66h                                     ; 09a5  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09a8  EAX := EAX + EBX
    DB 0C3h                                ; 09ab  return -- NEAR, see the note above

; gain 7
@@G3_06:
    DB 66h                                     ; 09ac  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09af  EAX := EAX + EBX
    DB 66h                                     ; 09b2  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09b5  EAX := EAX + EBX
    DB 0C3h                                ; 09b8  return -- NEAR, see the note above

; gain 15
@@G3_07:
    DB 66h                                     ; 09b9  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09bc  EAX := EAX + EBX
    DB 66h                                     ; 09bf  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09c2  EAX := EAX + EBX
    DB 66h                                     ; 09c5  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09c8  EAX := EAX + EBX
    DB 0C3h                                ; 09cb  return -- NEAR, see the note above

; gain 2
@@G3_08:
    DB 66h, 03h, 0C3h                      ; 09cc  EAX := EAX + EBX
    DB 0C3h                                ; 09cf  return -- NEAR, see the note above

; gain 10
@@G3_09:
    DB 66h, 03h, 0C3h                      ; 09d0  EAX := EAX + EBX
    DB 66h                                     ; 09d3  EBX := EBX shl 1
    SHL BX,1
    DB 66h                                     ; 09d6  EBX := EBX shl 2
    SHL BX,2
    DB 66h, 03h, 0C3h                      ; 09da  EAX := EAX + EBX
    DB 0C3h                                ; 09dd  return -- NEAR, see the note above

; gain 6
@@G3_10:
    DB 66h, 03h, 0C3h                      ; 09de  EAX := EAX + EBX
    DB 66h                                     ; 09e1  EBX := EBX shl 1
    SHL BX,1
    DB 66h                                     ; 09e4  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09e7  EAX := EAX + EBX
    DB 0C3h                                ; 09ea  return -- NEAR, see the note above

; gain 14
@@G3_11:
    DB 66h, 03h, 0C3h                      ; 09eb  EAX := EAX + EBX
    DB 66h                                     ; 09ee  EBX := EBX shl 1
    SHL BX,1
    DB 66h                                     ; 09f1  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09f4  EAX := EAX + EBX
    DB 66h                                     ; 09f7  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 09fa  EAX := EAX + EBX
    DB 0C3h                                ; 09fd  return -- NEAR, see the note above

; gain 4
@@G3_12:
    DB 66h, 03h, 0C3h                      ; 09fe  EAX := EAX + EBX
    DB 66h                                     ; 0a01  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a04  EAX := EAX + EBX
    DB 0C3h                                ; 0a07  return -- NEAR, see the note above

; gain 12
@@G3_13:
    DB 66h, 03h, 0C3h                      ; 0a08  EAX := EAX + EBX
    DB 66h                                     ; 0a0b  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a0e  EAX := EAX + EBX
    DB 66h                                     ; 0a11  EBX := EBX shl 1
    SHL BX,1
    DB 66h                                     ; 0a14  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a17  EAX := EAX + EBX
    DB 0C3h                                ; 0a1a  return -- NEAR, see the note above

; gain 8
@@G3_14:
    DB 66h, 03h, 0C3h                      ; 0a1b  EAX := EAX + EBX
    DB 66h                                     ; 0a1e  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a21  EAX := EAX + EBX
    DB 66h                                     ; 0a24  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a27  EAX := EAX + EBX
    DB 0C3h                                ; 0a2a  return -- NEAR, see the note above

; gain 16
@@G3_15:
    DB 66h, 03h, 0C3h                      ; 0a2b  EAX := EAX + EBX
    DB 66h                                     ; 0a2e  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a31  EAX := EAX + EBX
    DB 66h                                     ; 0a34  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a37  EAX := EAX + EBX
    DB 66h                                     ; 0a3a  EBX := EBX shl 1
    SHL BX,1
    DB 66h, 03h, 0C3h                      ; 0a3d  EAX := EAX + EBX
    DB 0C3h                                ; 0a40  return -- NEAR, see the note above

; the family's pointer table -- SetGains indexes it by volume
@@G3Table:
    DW OFFSET @@G3_00
    DW OFFSET @@G3_01
    DW OFFSET @@G3_02
    DW OFFSET @@G3_03
    DW OFFSET @@G3_04
    DW OFFSET @@G3_05
    DW OFFSET @@G3_06
    DW OFFSET @@G3_07
    DW OFFSET @@G3_08
    DW OFFSET @@G3_09
    DW OFFSET @@G3_10
    DW OFFSET @@G3_11
    DW OFFSET @@G3_12
    DW OFFSET @@G3_13
    DW OFFSET @@G3_14
    DW OFFSET @@G3_15

; ---- the patch area ----------------------------------------------
; Eight slots, one per channel. SetGains overwrites each with the
; address of a cell; the mixer CALLs through them. They start at the
; family-1 unity cell, so an unconfigured mixer passes samples through
; unchanged rather than silencing them.
@@Patch:
    DW OFFSET @@G1_00                      ; 0a61  slot 0
    DW OFFSET @@G1_00                      ; 0a63  slot 1
    DW OFFSET @@G1_00                      ; 0a65  slot 2
    DW OFFSET @@G1_00                      ; 0a67  slot 3
    DW OFFSET @@G1_00                      ; 0a69  slot 4
    DW OFFSET @@G1_00                      ; 0a6b  slot 5
    DW OFFSET @@G1_00                      ; 0a6d  slot 6
    DW OFFSET @@G1_00                      ; 0a6f  slot 7

; ---- the 4-bit bit-reversal permutation ---------------------------
; Channels 1,3,5,7 index family 3 through this rather than directly.
; Every entry is bitrev4(index), verified below.
@@BitRev:
    DB 000h, 008h, 004h, 00Ch, 002h, 00Ah, 006h, 00Eh, 001h, 009h, 005h, 00Dh, 003h, 00Bh, 007h, 00Fh

; ---- the patcher -------------------------------------------------
; Reference Pascal, for reading only -- the real thing is below:
; GainSlot[0] := G1Table[Volumes[0]];
; GainSlot[1] := G3Table[BitRev[Volumes[1]]];
; GainSlot[2] := G2Table[Volumes[2]];   ... and so on
@@Patcher:

; channel 0 -- attenuate
        MOV     AX,WORD PTR Volumes[0]     ; 0a81  the channel's volume
        DB 03h, 0C0h                       ; 0a84  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G1Table
        DB 03h, 0D8h                       ; 0a89  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0a8b  the cell's address
        MOV     WORD PTR CS:[@@Patch],AX   ; 0a8e  patch slot 0

; channel 1 -- permuted integer ladder
        MOV     AX,WORD PTR Volumes[2]     ; 0a92  the channel's volume
        MOV     BX,OFFSET @@BitRev
        DB 03h, 0D8h                       ; 0a98  ADD BX,AX -- see the encoding note
        MOV     AL,CS:[BX]                 ; 0a9a  permute the index
        DB 03h, 0C0h                       ; 0a9d  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G3Table
        DB 03h, 0D8h                       ; 0aa2  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0aa4  the cell's address
        MOV     WORD PTR CS:[@@Patch+2],AX ; 0aa7  patch slot 1

; channel 2 -- boost
        MOV     AX,WORD PTR Volumes[4]     ; 0aab  the channel's volume
        DB 03h, 0C0h                       ; 0aae  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G2Table
        DB 03h, 0D8h                       ; 0ab3  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0ab5  the cell's address
        MOV     WORD PTR CS:[@@Patch+4],AX ; 0ab8  patch slot 2

; channel 3 -- permuted integer ladder
        MOV     AX,WORD PTR Volumes[6]     ; 0abc  the channel's volume
        MOV     BX,OFFSET @@BitRev
        DB 03h, 0D8h                       ; 0ac2  ADD BX,AX -- see the encoding note
        MOV     AL,CS:[BX]                 ; 0ac4  permute the index
        DB 03h, 0C0h                       ; 0ac7  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G3Table
        DB 03h, 0D8h                       ; 0acc  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0ace  the cell's address
        MOV     WORD PTR CS:[@@Patch+6],AX ; 0ad1  patch slot 3

; channel 4 -- attenuate
        MOV     AX,WORD PTR Volumes[8]     ; 0ad5  the channel's volume
        DB 03h, 0C0h                       ; 0ad8  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G1Table
        DB 03h, 0D8h                       ; 0add  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0adf  the cell's address
        MOV     WORD PTR CS:[@@Patch+8],AX ; 0ae2  patch slot 4

; channel 5 -- permuted integer ladder
        MOV     AX,WORD PTR Volumes[10]    ; 0ae6  the channel's volume
        MOV     BX,OFFSET @@BitRev
        DB 03h, 0D8h                       ; 0aec  ADD BX,AX -- see the encoding note
        MOV     AL,CS:[BX]                 ; 0aee  permute the index
        DB 03h, 0C0h                       ; 0af1  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G3Table
        DB 03h, 0D8h                       ; 0af6  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0af8  the cell's address
        MOV     WORD PTR CS:[@@Patch+10],AX ; 0afb  patch slot 5

; channel 6 -- boost
        MOV     AX,WORD PTR Volumes[12]    ; 0aff  the channel's volume
        DB 03h, 0C0h                       ; 0b02  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G2Table
        DB 03h, 0D8h                       ; 0b07  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0b09  the cell's address
        MOV     WORD PTR CS:[@@Patch+12],AX ; 0b0c  patch slot 6

; channel 7 -- permuted integer ladder
        MOV     AX,WORD PTR Volumes[14]    ; 0b10  the channel's volume
        MOV     BX,OFFSET @@BitRev
        DB 03h, 0D8h                       ; 0b16  ADD BX,AX -- see the encoding note
        MOV     AL,CS:[BX]                 ; 0b18  permute the index
        DB 03h, 0C0h                       ; 0b1b  ADD AX,AX -- word index
        MOV     BX,OFFSET @@G3Table
        DB 03h, 0D8h                       ; 0b20  ADD BX,AX -- see the encoding note
        MOV     AX,CS:[BX]                 ; 0b22  the cell's address
        MOV     WORD PTR CS:[@@Patch+14],AX ; 0b25  patch slot 7
    DB 0C3h                                ; 0b29  RET -- NEAR, the
; patcher's own; explicit
; because the procedure
; continues past it

;  ==================================================================
;     THE TWO FILTER CHAINS AND THEIR WRAPPER.
;
;     Still inside this procedure: these reach their gain cells through
;     @@Patch above, and labels do not cross procedures.
;
;     WHAT EACH CHAIN COMPUTES. A two-tap filter built entirely out of the
;     patched gain cells -- there is no multiply anywhere in it:
;
;         a := Gain_0(sample)      first tap, on the new sample
;         b := Gain_1(a)
;         c := Gain_2(previous)    second tap, on the held-over one
;         d := Gain_3(c)
;         result := (b + d) shr 2  then clamped to signed 16 bits
;
;     THE PREVIOUS SAMPLE LIVES IN THE INSTRUCTION STREAM. 0b2f loads it as
;     a 32-bit IMMEDIATE and 0b3a writes the new one back over those four
;     bytes. So the filter state is not in DGROUP and not in a register
;     across calls -- it is the constant the next call will load, which
;     costs no memory reference to read. Same instinct as the shared poll's
;     guard byte at CS:$104b, for speed rather than for hiding.
;
;     THE CLAMP READS BACKWARDS. The fast path is CMP / JNB / RET: one
;     comparison and a one-byte jump over the return for the overwhelmingly
;     common in-range case. Only a value at or above $8000 falls through to
;     the second test, which separates real overflow from a negative number
;     that merely looks large unsigned.
;
;     WHY SO MANY DB. Turbo Pascal assembles several of these mnemonics to a
;     different ENCODING of the same instruction than the original's
;     assembler chose -- see the note on the cells above, and note that a
;     written RET here would come out CB rather than C3 because this
;     procedure is far. The bytes are explicit and the mnemonic is in the
;     comment. Jumps and near calls keep their labels: those displacements
;     are self-relative, so they are the compiler's to compute.
;     ==================================================================

; ---- chain A, slots 0..3 -----------------------------------------
@@FilterA:
    DB 66h, 98h                            ; 0b2a  CWDE -- sign-extend AX into EAX
    DB 66h, 8Bh, 0C8h                      ; 0b2c  MOV  ECX,EAX -- keep the sample
    DB 66h, 0BBh                           ; 0b2f  MOV  EBX,imm32 ...
@@PrevA1:
    DD -1                                  ; 0b31  ... and the immediate IS the state
    DB 2Eh, 0FFh, 16h                      ; 0b35  CALL WORD PTR CS:[@@Patch]
    DW OFFSET @@Patch
    DB 66h, 2Eh, 0A3h                      ; 0b3a  MOV  CS:[@@PrevA1],EAX
    DW OFFSET @@PrevA1
    DB 66h, 8Bh, 0D8h                      ; 0b3f  MOV  EBX,EAX
    DB 2Eh, 0FFh, 16h                      ; 0b42  CALL WORD PTR CS:[@@Patch+2]
    DW OFFSET @@Patch+2
    DB 66h, 91h                            ; 0b47  XCHG EAX,ECX
    DB 66h, 0BBh                           ; 0b49  MOV  EBX,imm32 ...
@@PrevA2:
    DD -1                                  ; 0b4b  ... the second tap's state
    DB 2Eh, 0FFh, 16h                      ; 0b4f  CALL WORD PTR CS:[@@Patch+4]
    DW OFFSET @@Patch+4
    DB 66h, 2Eh, 0A3h                      ; 0b54  MOV  CS:[@@PrevA2],EAX
    DW OFFSET @@PrevA2
    DB 66h, 8Bh, 0D8h                      ; 0b59  MOV  EBX,EAX
    DB 2Eh, 0FFh, 16h                      ; 0b5c  CALL WORD PTR CS:[@@Patch+6]
    DW OFFSET @@Patch+6
    DB 66h, 03h, 0C1h                      ; 0b61  ADD  EAX,ECX -- the two taps
    DB 66h, 0C1h, 0F8h, 02h                ; 0b64  SAR  EAX,2
    DB 66h, 3Dh                            ; 0b68  CMP  EAX,00008000h
    DD 8000h
    JNB     @@ClampA                       ; 0b6e  out of range -- go and sort it
@@DoneA:
    DB 0C3h                                ; 0b70  RET -- NEAR, the common path
@@ClampA:
    DB 66h, 3Dh                            ; 0b71  CMP  EAX,FFFF8000h
    DD 0FFFF8000h
    JNB     @@DoneA                        ; 0b77  negative but in range after all
    DB 66h, 23h, 0C0h                      ; 0b79  AND  EAX,EAX -- test the sign
    JS      @@LowA
    DB 66h, 0B8h                           ; 0b7e  MOV  EAX,00007FFFh
    DD 7FFFh
    JMP     @@DoneA
@@LowA:
    DB 66h, 0B8h                           ; 0b86  MOV  EAX,FFFF8000h
    DD 0FFFF8000h
    JMP     @@DoneA

; ---- chain B, slots 4..7 -- the same again -----------------------
@@FilterB:
    DB 66h, 98h                            ; 0b8e  CWDE
    DB 66h, 8Bh, 0C8h                      ; 0b90  MOV  ECX,EAX
    DB 66h, 0BBh                           ; 0b93  MOV  EBX,imm32 ...
@@PrevB1:
    DD -1
    DB 2Eh, 0FFh, 16h                      ; 0b99  CALL WORD PTR CS:[@@Patch+8]
    DW OFFSET @@Patch+8
    DB 66h, 2Eh, 0A3h                      ; 0b9e  MOV  CS:[@@PrevB1],EAX
    DW OFFSET @@PrevB1
    DB 66h, 8Bh, 0D8h                      ; 0ba3  MOV  EBX,EAX
    DB 2Eh, 0FFh, 16h                      ; 0ba6  CALL WORD PTR CS:[@@Patch+10]
    DW OFFSET @@Patch+10
    DB 66h, 91h                            ; 0bab  XCHG EAX,ECX
    DB 66h, 0BBh                           ; 0bad  MOV  EBX,imm32 ...
@@PrevB2:
    DD -1
    DB 2Eh, 0FFh, 16h                      ; 0bb3  CALL WORD PTR CS:[@@Patch+12]
    DW OFFSET @@Patch+12
    DB 66h, 2Eh, 0A3h                      ; 0bb8  MOV  CS:[@@PrevB2],EAX
    DW OFFSET @@PrevB2
    DB 66h, 8Bh, 0D8h                      ; 0bbd  MOV  EBX,EAX
    DB 2Eh, 0FFh, 16h                      ; 0bc0  CALL WORD PTR CS:[@@Patch+14]
    DW OFFSET @@Patch+14
    DB 66h, 03h, 0C1h                      ; 0bc5  ADD  EAX,ECX
    DB 66h, 0C1h, 0F8h, 02h                ; 0bc8  SAR  EAX,2
    DB 66h, 3Dh                            ; 0bcc  CMP  EAX,00008000h
    DD 8000h
    JNB     @@ClampB
@@DoneB:
    DB 0C3h                                ; 0bd4  RET -- NEAR
@@ClampB:
    DB 66h, 3Dh                            ; 0bd5  CMP  EAX,FFFF8000h
    DD 0FFFF8000h
    JNB     @@DoneB
    DB 66h, 23h, 0C0h                      ; 0bdd  AND  EAX,EAX
    JS      @@LowB
    DB 66h, 0B8h                           ; 0be2  MOV  EAX,00007FFFh
    DD 7FFFh
    JMP     @@DoneB
@@LowB:
    DB 66h, 0B8h                           ; 0bea  MOV  EAX,FFFF8000h
    DD 0FFFF8000h
    JMP     @@DoneB

;  Run a pair of samples through both filter chains.
;
;     AX carries one sample and BX the other, in and out. All the shuffling
;     is because both chains take their input in AX and return it there: BX
;     is parked on the stack across the first call, then swapped in for the
;     second while the first result takes its place.
;
;     The final RET is Pascal's own epilogue rather than a written one -- an
;     `assembler` procedure always gets one, so writing it would emit a
;     second. That is also what puts a NEAR-looking C3 at 0c00 even though
;     this procedure is far: it is the procedure's own return.
@@FilterPair:
    DB 53h                                 ; 0bf2  PUSH BX -- park the second sample
    CALL    @@FilterA
    DB 5Bh                                 ; 0bf6  POP  BX
    DB 50h                                 ; 0bf7  PUSH AX -- park the first result
    DB 8Bh, 0C3h                           ; 0bf8  MOV  AX,BX
    CALL    @@FilterB
    DB 8Bh, 0D8h                           ; 0bfd  MOV  BX,AX
    DB 58h                                 ; 0bff  POP  AX
    DB 0C3h                                ; 0c00  RET -- NEAR, and explicit now that the
; downmix follows. It was the procedure's
; own epilogue while this was the last
; block, which is how the RETF-vs-RET
; problem surfaced here.

;  ==================================================================
;     THE KERNEL CONFIGURATOR. Frameless, near, and the reason
;
;     From NumChannels it derives two numbers -- 3 * (32 - Channels), the
;     length of the unrolled ADDs to skip, and Channels * 2, the row stride
;     -- and writes them into five sites. Then it picks a mixing method by
;     writing a JMP displacement, and switches the filter stage on or off by
;     writing either C3h or 90h into two more.
;
;     THE TWO LOOP BIASES DIFFER, 70h AND 74h, and that is not a typo: the
;     stereo loop's tail is four bytes longer than the mono one's, so the
;     same "jump back into the unrolled run" needs a different constant. Get
;     them the same way round and one of the two loops reads four bytes into
;     the middle of an instruction.
;
;     It ends by calling the gain patcher at 0a81, so one call configures
;     both the kernels and the volumes.
;     ==================================================================
@@Configure:
    MOV     CX,NumChannels                 ; 0c01  how many channels are live
    DB 0BAh, 20h, 00h                      ; 0c05  MOV  DX,32
    DB 2Bh, 0D1h                           ; 0c08  SUB  DX,CX -- how many are not
    DB 8Bh, 0DAh                           ; 0c0a  MOV  BX,DX
    DB 03h, 0D2h                           ; 0c0c  ADD  DX,DX
    DB 03h, 0D3h                           ; 0c0e  ADD  DX,BX -- DX := 3 * (32 - N)
    DB 03h, 0C9h                           ; 0c10  ADD  CX,CX -- CX := 2 * N

    DB 2Eh, 88h, 16h                       ; 0c12  MOV  CS:[@@UnrollEntry],DL
    DW OFFSET @@UnrollEntry
    DB 2Eh, 89h, 0Eh                       ; 0c17  MOV  CS:[@@Stride],CX
    DW OFFSET @@Stride

    DB 8Bh, 0DAh                           ; 0c1c  MOV  BX,DX
    DB 83h, 0EBh, 70h                      ; 0c1e  SUB  BX,70h -- the MONO bias
    DB 90h                                 ; 0c21  NOP -- jump-sizing padding
    DB 2Eh, 88h, 1Eh                       ; 0c22  MOV  CS:[@@MonoLoop],BL
    DW OFFSET @@MonoLoop

    DB 8Bh, 0DAh                           ; 0c27  MOV  BX,DX
    DB 83h, 0EBh, 74h                      ; 0c29  SUB  BX,74h -- the STEREO bias
    DB 90h                                 ; 0c2c  NOP
    DB 2Eh, 88h, 1Eh                       ; 0c2d  MOV  CS:[@@StereoLoop],BL
    DW OFFSET @@StereoLoop

    DB 2Eh, 89h, 0Eh                       ; 0c32  MOV  CS:[@@Stride2],CX
    DW OFFSET @@Stride2
    DB 2Eh, 89h, 0Eh                       ; 0c37  MOV  CS:[@@Stride3],CX
    DW OFFSET @@Stride3

; The mixing method, as a JMP displacement: 0, 12h, 30h or 5Ah.
    MOV     DL,MixMethod
    DB 0BBh, 00h, 00h                      ; 0c40  MOV  BX,0
    DB 22h, 0D2h                           ; 0c43  AND  DL,DL
    JZ      @@GotMethod
    DB 90h                                 ; 0c47  NOP
    DB 90h                                 ; 0c48  NOP
    DB 0BBh, 12h, 00h                      ; 0c49  MOV  BX,12h
    DB 0FEh, 0CAh                          ; 0c4c  DEC  DL
    JZ      @@GotMethod
    DB 90h                                 ; 0c50  NOP
    DB 90h                                 ; 0c51  NOP
    DB 0BBh, 30h, 00h                      ; 0c52  MOV  BX,30h
    DB 0FEh, 0CAh                          ; 0c55  DEC  DL
    JZ      @@GotMethod
    DB 90h                                 ; 0c59  NOP
    DB 90h                                 ; 0c5a  NOP
    DB 0BBh, 5Ah, 00h                      ; 0c5b  MOV  BX,5Ah
@@GotMethod:
    DB 2Eh, 89h, 1Eh                       ; 0c5e  MOV  CS:[@@MethodJmp],BX
    DW OFFSET @@MethodJmp

; The filter switch: C3h returns, 90h falls through into the chain.
    DB 0B4h, 0C3h                          ; 0c63  MOV  AH,0C3h -- RET, filters off
    MOV     AL,DoBassPower
    DB 22h, 0C0h                           ; 0c68  AND  AL,AL
    JZ      @@GotSwitch
    DB 90h                                 ; 0c6c  NOP
    DB 90h                                 ; 0c6d  NOP
    DB 0B4h, 90h                           ; 0c6e  MOV  AH,90h -- NOP, filters on
@@GotSwitch:
    DB 2Eh, 88h, 26h                       ; 0c70  MOV  CS:[@@FilterSwitch],AH
    DW OFFSET @@FilterSwitch
    DB 2Eh, 88h, 26h                       ; 0c75  MOV  CS:[@@FilterSwitch2],AH
    DW OFFSET @@FilterSwitch2

    CALL    @@Patcher                      ; 0c7a  and the gains, while we are here
    DB 0C3h                                ; 0c7d  RET -- NEAR

;  ==================================================================
;     THE 32-WAY UNROLLED DOWNMIX. Frameless; falls into the
;
;     SI walks a row of 32 channel samples, words, and the row is summed
;     into two accumulators. Which accumulator a channel goes to follows
;     `n mod 4`: 0 and 3 to AX, 1 and 2 to BX. That is a four-channel
;     panning interleave, not an alternation -- reading it as one is the
;     easy mistake.
;
;     THREE OF THE CONFIGURATOR'S NINE PATCH SITES ARE IN HERE, and between
;     them they remove every per-sample decision from the loop:
;
;       @@UnrollEntry  0c83  the displacement of the JMP at 0c82. The
;                            configurator writes (32 - Channels) * 3, which
;                            skips exactly the ADDs for channels that are
;                            not there. THE CHANNEL COUNT COSTS NOTHING --
;                            no counter, no compare, no branch.
;       @@Stride       0ce3  the immediate of ADD SI, so advancing to the
;                            next row is one instruction whatever the width.
;       @@FilterSwitch 0cf6  either 90h or C3h. A NOP falls through into
;                            the filter chain; a RET returns instead. That
;                            is how a whole filter stage is switched off
;                            without a test.
;
;     THE SATURATING ADD AT 0ce8 USES THE OVERFLOW DIRECTION. After ADD
;     AX,BX, JNO takes the common path. On overflow the stored sign is the
;     INVERSE of the true one, so SF set means the true result was too
;     POSITIVE -- hence JS goes to 7FFFh and the fall-through to 8001h.
;
;     Note 8001h, not 8000h. One off the true minimum, and consistently so;
;     presumably to keep the range symmetric about zero.
;     ==================================================================
@@DownMix:
    DB 8Bh, 04h                            ; 0c7e  MOV  AX,[SI] -- channel 0
    DB 33h, 0DBh                           ; 0c80  XOR  BX,BX
    DB 0EBh                                ; 0c82  JMP  SHORT ...
@@UnrollEntry:
    DB 0                                   ; 0c83  ... and the displacement is patched
    DB 03h, 44h, 3Eh                       ; 0c84  ADD  AX,[SI+3eh] -- ch 31
    DB 03h, 5Ch, 3Ch                       ; 0c87  ADD  BX,[SI+3ch] -- ch 30
    DB 03h, 5Ch, 3Ah                       ; 0c8a  ADD  BX,[SI+3ah] -- ch 29
    DB 03h, 44h, 38h                       ; 0c8d  ADD  AX,[SI+38h] -- ch 28
    DB 03h, 44h, 36h                       ; 0c90  ADD  AX,[SI+36h] -- ch 27
    DB 03h, 5Ch, 34h                       ; 0c93  ADD  BX,[SI+34h] -- ch 26
    DB 03h, 5Ch, 32h                       ; 0c96  ADD  BX,[SI+32h] -- ch 25
    DB 03h, 44h, 30h                       ; 0c99  ADD  AX,[SI+30h] -- ch 24
    DB 03h, 44h, 2Eh                       ; 0c9c  ADD  AX,[SI+2eh] -- ch 23
    DB 03h, 5Ch, 2Ch                       ; 0c9f  ADD  BX,[SI+2ch] -- ch 22
    DB 03h, 5Ch, 2Ah                       ; 0ca2  ADD  BX,[SI+2ah] -- ch 21
    DB 03h, 44h, 28h                       ; 0ca5  ADD  AX,[SI+28h] -- ch 20
    DB 03h, 44h, 26h                       ; 0ca8  ADD  AX,[SI+26h] -- ch 19
    DB 03h, 5Ch, 24h                       ; 0cab  ADD  BX,[SI+24h] -- ch 18
    DB 03h, 5Ch, 22h                       ; 0cae  ADD  BX,[SI+22h] -- ch 17
    DB 03h, 44h, 20h                       ; 0cb1  ADD  AX,[SI+20h] -- ch 16
    DB 03h, 44h, 1Eh                       ; 0cb4  ADD  AX,[SI+1eh] -- ch 15
    DB 03h, 5Ch, 1Ch                       ; 0cb7  ADD  BX,[SI+1ch] -- ch 14
    DB 03h, 5Ch, 1Ah                       ; 0cba  ADD  BX,[SI+1ah] -- ch 13
    DB 03h, 44h, 18h                       ; 0cbd  ADD  AX,[SI+18h] -- ch 12
    DB 03h, 44h, 16h                       ; 0cc0  ADD  AX,[SI+16h] -- ch 11
    DB 03h, 5Ch, 14h                       ; 0cc3  ADD  BX,[SI+14h] -- ch 10
    DB 03h, 5Ch, 12h                       ; 0cc6  ADD  BX,[SI+12h] -- ch 9
    DB 03h, 44h, 10h                       ; 0cc9  ADD  AX,[SI+10h] -- ch 8
    DB 03h, 44h, 0Eh                       ; 0ccc  ADD  AX,[SI+0eh] -- ch 7
    DB 03h, 5Ch, 0Ch                       ; 0ccf  ADD  BX,[SI+0ch] -- ch 6
    DB 03h, 5Ch, 0Ah                       ; 0cd2  ADD  BX,[SI+0ah] -- ch 5
    DB 03h, 44h, 08h                       ; 0cd5  ADD  AX,[SI+08h] -- ch 4
    DB 03h, 44h, 06h                       ; 0cd8  ADD  AX,[SI+06h] -- ch 3
    DB 03h, 5Ch, 04h                       ; 0cdb  ADD  BX,[SI+04h] -- ch 2
    DB 03h, 5Ch, 02h                       ; 0cde  ADD  BX,[SI+02h] -- ch 1
    DB 81h, 0C6h                           ; 0ce1  ADD  SI,imm16 ...
@@Stride:
    DW 1234h                               ; 0ce3  ... the row stride, patched
    DB 0E9h                                ; 0ce5  JMP  NEAR ...
@@MethodJmp:
    DW 0FA5Dh                              ; 0ce6  ... displacement patched to one of
; 0, 12h, 30h or 5Ah -- the mixing
; method. The value here is the
; original's own unpatched one; it
; resolves to 0745, which is a RETF,
; so an unconfigured mixer returns.
; Emitted verbatim either way.
    DB 03h, 0C3h                           ; 0ce8  ADD  AX,BX -- the two accumulators
    JNO     @@Saturated                    ; 0cea  the common path
    JS      @@TooHigh                      ; 0cec  sign INVERTED by the overflow
    DB 0B8h, 01h, 80h                      ; 0cee  MOV  AX,8001h -- see the note
    JMP     @@Saturated
@@TooHigh:
    DB 0B8h, 0FFh, 7Fh                     ; 0cf3  MOV  AX,7FFFh
@@Saturated:
@@FilterSwitch:
    DB 90h                                 ; 0cf6  NOP or C3h -- the filter switch
    JMP     @@FilterA                      ; 0cf7  tail call into the filter chain

;  ==================================================================
;     THE OTHER THREE MIXING METHODS.
;
;     @@MethodJmp above selects between these by displacement: 0 falls
;     straight through to the mono path at 0ce8, and 12h, 30h and 5Ah land
;     here. All three end at @@StereoTail, which tail-calls the filter PAIR
;     rather than the single chain the mono path uses.
;
;     METHOD 1, 0cfa -- double each side independently. The cheapest way to
;     make up the headroom the accumulators were summed with.
;
;     METHOD 2, 0d18 -- cross-feed by an EXACT 17-BIT AVERAGE, and this is
;     the piece worth stopping on:
;
;         MOV DX,AX / ADD DX,BX / JNO +4 / RCR DX,1 / JMP +2 / SAR DX,1
;
;     A signed 16-bit sum of two 16-bit values needs 17 bits. When it
;     overflows, the seventeenth bit is in CF -- so RCR rotates it back in as
;     the new top bit and the halved result is EXACT rather than clamped.
;     Where there was no overflow, SAR halves and preserves the sign. Two
;     instructions and a branch to get a correct average with no widening.
;
;     METHOD 3, 0d42 -- halve both sides first, then cross-feed 1.5x of
;     their sum: DX := (AX+BX); DX := DX + DX shr 1. The pre-halving is what
;     buys the room for the 1.5.
;     ==================================================================
@@Method1:
    DB 03h, 0DBh                           ; 0cfa  ADD  BX,BX
    DB 71h, 0Ah                            ; 0cfc  JNO  -- in range, done
    DB 78h, 05h                            ; 0cfe  JS   -- too high, see above
    DB 0BBh, 01h, 80h                      ; 0d00  MOV  BX,8001h
    DB 0EBh, 03h                           ; 0d03  JMP  past the other arm
    DB 0BBh, 0FFh, 7Fh                     ; 0d05  MOV  BX,7FFFh
    DB 03h, 0C0h                           ; 0d08  ADD  AX,AX
    DB 71h, 0Ah                            ; 0d0a  JNO  -- in range, done
    DB 78h, 05h                            ; 0d0c  JS   -- too high, see above
    DB 0B8h, 01h, 80h                      ; 0d0e  MOV  AX,8001h
    DB 0EBh, 03h                           ; 0d11  JMP  past the other arm
    DB 0B8h, 0FFh, 7Fh                     ; 0d13  MOV  AX,7FFFh
    JMP     @@FilterSwitch2
@@Method2:
    DB 8Bh, 0D0h                           ; 0d18  MOV  DX,AX
    DB 03h, 0D3h                           ; 0d1a  ADD  DX,BX -- 17 bits
    DB 71h, 04h                            ; 0d1c  JNO  -- it fitted
    DB 0D1h, 0DAh                          ; 0d1e  RCR  DX,1 -- CF is bit 16
    DB 0EBh, 02h                           ; 0d20  JMP
    DB 0D1h, 0FAh                          ; 0d22  SAR  DX,1 -- signed halve
    DB 03h, 0C2h                           ; 0d24  ADD  AX,DX
    DB 71h, 0Ah                            ; 0d26  JNO  -- in range, done
    DB 78h, 05h                            ; 0d28  JS   -- too high, see above
    DB 0B8h, 01h, 80h                      ; 0d2a  MOV  AX,8001h
    DB 0EBh, 03h                           ; 0d2d  JMP  past the other arm
    DB 0B8h, 0FFh, 7Fh                     ; 0d2f  MOV  AX,7FFFh
    DB 03h, 0DAh                           ; 0d32  ADD  BX,DX
    DB 71h, 0Ah                            ; 0d34  JNO  -- in range, done
    DB 78h, 05h                            ; 0d36  JS   -- too high, see above
    DB 0BBh, 01h, 80h                      ; 0d38  MOV  BX,8001h
    DB 0EBh, 03h                           ; 0d3b  JMP  past the other arm
    DB 0BBh, 0FFh, 7Fh                     ; 0d3d  MOV  BX,7FFFh
    JMP     @@FilterSwitch2
@@Method3:
    DB 0D1h, 0F8h                          ; 0d42  SAR  AX,1
    DB 0D1h, 0FBh                          ; 0d44  SAR  BX,1
    DB 8Bh, 0D0h                           ; 0d46  MOV  DX,AX
    DB 03h, 0D3h                           ; 0d48  ADD  DX,BX
    DB 8Bh, 0CAh                           ; 0d4a  MOV  CX,DX -- keep the sum
    DB 0D1h, 0FAh                          ; 0d4c  SAR  DX,1
    DB 03h, 0D1h                           ; 0d4e  ADD  DX,CX -- 1.5x the sum
    DB 71h, 0Ah                            ; 0d50  JNO  -- in range, done
    DB 78h, 05h                            ; 0d52  JS   -- too high, see above
    DB 0BAh, 01h, 80h                      ; 0d54  MOV  DX,8001h
    DB 0EBh, 03h                           ; 0d57  JMP  past the other arm
    DB 0BAh, 0FFh, 7Fh                     ; 0d59  MOV  DX,7FFFh
    DB 03h, 0C2h                           ; 0d5c  ADD  AX,DX
    DB 71h, 0Ah                            ; 0d5e  JNO  -- in range, done
    DB 78h, 05h                            ; 0d60  JS   -- too high, see above
    DB 0B8h, 01h, 80h                      ; 0d62  MOV  AX,8001h
    DB 0EBh, 03h                           ; 0d65  JMP  past the other arm
    DB 0B8h, 0FFh, 7Fh                     ; 0d67  MOV  AX,7FFFh
    DB 03h, 0DAh                           ; 0d6a  ADD  BX,DX
    DB 71h, 0Ah                            ; 0d6c  JNO  -- in range, done
    DB 78h, 05h                            ; 0d6e  JS   -- too high, see above
    DB 0BBh, 01h, 80h                      ; 0d70  MOV  BX,8001h
    DB 0EBh, 03h                           ; 0d73  JMP  past the other arm
    DB 0BBh, 0FFh, 7Fh                     ; 0d75  MOV  BX,7FFFh
@@FilterSwitch2:
    DB 90h                                 ; 0d78  NOP or C3h -- the same switch as
; 0cf6, written by the same store
    JMP     @@FilterPair                   ; 0d79  tail call -- BOTH chains, not one

;  ==================================================================
;     THE MONO OUTPUT LOOP. This is what SelectOutput's two entry points
;
;     Per sample: sum the 32 channels into two accumulators, add them,
;     saturate, convert to unsigned, store one byte, advance. CX is the
;     sample count and LOOP drives it.
;
;     IT IS ENTERED AT THE BOTTOM. The JMP at 0d7d goes to @@MonoPrime,
;     which loads channels 0 and 1 into the two accumulators before any
;     ADD runs -- so the unrolled run covers channels 31 down to 2, THIRTY
;     adds rather than thirty-one. Priming instead of zeroing saves two
;     instructions per sample.
;
;     TWO MORE PATCH SITES, AND THE SECOND IS THE NICE ONE:
;
;       @@Stride2   0ddb  the row stride, as in the downmix.
;       @@MonoLoop  0dee  THE DISPLACEMENT OF THE LOOP INSTRUCTION. The
;                         configurator writes 3 * (32 - Channels) - 70h, so
;                         the backward jump lands partway INTO the unrolled
;                         run and the absent channels are never touched.
;                         The loop's own branch target is the channel count.
;
;     THE OUTPUT CONVERSION IS ONE INSTRUCTION. `XOR AH,80h` flips the sign
;     bit of the high byte, which turns a signed 16-bit sample into the
;     unsigned 8-bit one a DAC wants -- taking the high byte IS the
;     downshift, so there is no shift at all. Then MOV ES:[DI],AH.
;     ==================================================================
@@MonoMix:
    DB 41h                                 ; 0d7c  INC  CX -- LOOP counts down
    JMP     @@MonoPrime                    ; 0d7d  enter at the bottom, see above
    DB 03h, 44h, 3Eh                       ; 0d7f  ADD  AX,[SI+3eh] -- ch 31
    DB 03h, 5Ch, 3Ch                       ; 0d82  ADD  BX,[SI+3ch] -- ch 30
    DB 03h, 5Ch, 3Ah                       ; 0d85  ADD  BX,[SI+3ah] -- ch 29
    DB 03h, 44h, 38h                       ; 0d88  ADD  AX,[SI+38h] -- ch 28
    DB 03h, 44h, 36h                       ; 0d8b  ADD  AX,[SI+36h] -- ch 27
    DB 03h, 5Ch, 34h                       ; 0d8e  ADD  BX,[SI+34h] -- ch 26
    DB 03h, 5Ch, 32h                       ; 0d91  ADD  BX,[SI+32h] -- ch 25
    DB 03h, 44h, 30h                       ; 0d94  ADD  AX,[SI+30h] -- ch 24
    DB 03h, 44h, 2Eh                       ; 0d97  ADD  AX,[SI+2eh] -- ch 23
    DB 03h, 5Ch, 2Ch                       ; 0d9a  ADD  BX,[SI+2ch] -- ch 22
    DB 03h, 5Ch, 2Ah                       ; 0d9d  ADD  BX,[SI+2ah] -- ch 21
    DB 03h, 44h, 28h                       ; 0da0  ADD  AX,[SI+28h] -- ch 20
    DB 03h, 44h, 26h                       ; 0da3  ADD  AX,[SI+26h] -- ch 19
    DB 03h, 5Ch, 24h                       ; 0da6  ADD  BX,[SI+24h] -- ch 18
    DB 03h, 5Ch, 22h                       ; 0da9  ADD  BX,[SI+22h] -- ch 17
    DB 03h, 44h, 20h                       ; 0dac  ADD  AX,[SI+20h] -- ch 16
    DB 03h, 44h, 1Eh                       ; 0daf  ADD  AX,[SI+1eh] -- ch 15
    DB 03h, 5Ch, 1Ch                       ; 0db2  ADD  BX,[SI+1ch] -- ch 14
    DB 03h, 5Ch, 1Ah                       ; 0db5  ADD  BX,[SI+1ah] -- ch 13
    DB 03h, 44h, 18h                       ; 0db8  ADD  AX,[SI+18h] -- ch 12
    DB 03h, 44h, 16h                       ; 0dbb  ADD  AX,[SI+16h] -- ch 11
    DB 03h, 5Ch, 14h                       ; 0dbe  ADD  BX,[SI+14h] -- ch 10
    DB 03h, 5Ch, 12h                       ; 0dc1  ADD  BX,[SI+12h] -- ch 9
    DB 03h, 44h, 10h                       ; 0dc4  ADD  AX,[SI+10h] -- ch 8
    DB 03h, 44h, 0Eh                       ; 0dc7  ADD  AX,[SI+0eh] -- ch 7
    DB 03h, 5Ch, 0Ch                       ; 0dca  ADD  BX,[SI+0ch] -- ch 6
    DB 03h, 5Ch, 0Ah                       ; 0dcd  ADD  BX,[SI+0ah] -- ch 5
    DB 03h, 44h, 08h                       ; 0dd0  ADD  AX,[SI+08h] -- ch 4
    DB 03h, 44h, 06h                       ; 0dd3  ADD  AX,[SI+06h] -- ch 3
    DB 03h, 5Ch, 04h                       ; 0dd6  ADD  BX,[SI+04h] -- ch 2
    DB 81h, 0C6h                           ; 0dd9  ADD  SI,imm16 ...
@@Stride2:
    DW 1234h                               ; 0ddb  ... the row stride, patched
    DB 03h, 0C3h                           ; 0ddd  ADD  AX,BX
    JO      @@MonoClamp
@@MonoStore:
    DB 80h, 0F4h, 80h                      ; 0de1  XOR  AH,80h -- signed to unsigned,
; and the high byte IS the downshift
    DB 26h, 88h, 25h                       ; 0de4  MOV  ES:[DI],AH -- one byte out
    DB 47h                                 ; 0de7  INC  DI
@@MonoPrime:
    DB 8Bh, 04h                            ; 0de8  MOV  AX,[SI]     -- channel 0
    DB 8Bh, 5Ch, 02h                       ; 0dea  MOV  BX,[SI+02h] -- channel 1
    DB 0E2h                                ; 0ded  LOOP ...
@@MonoLoop:
    DB 90h                                 ; 0dee  ... and the displacement is patched
    DB 0C3h                                ; 0def  RET -- NEAR
@@MonoClamp:
    JNS     @@MonoLow                      ; 0df0  sign NOT inverted here: this tests
; the accumulated sign, not the sum's
    DB 0B8h, 0FFh, 7Fh                     ; 0df2  MOV  AX,7FFFh
    JMP     @@MonoStore
@@MonoLow:
    DB 0B8h, 01h, 80h                      ; 0df7  MOV  AX,8001h
    JMP     @@MonoStore

;  ==================================================================
;     THE STEREO OUTPUT LOOP. The other of the two routines
;
;     Same shape as the mono loop -- primed entry at the bottom, unrolled
;     ADDs for channels 31 down to 2, LOOP driven by CX -- with the last
;     two patch sites:
;
;       @@Stride3      0e5b  the row stride.
;       @@StereoLoop   0e72  the LOOP displacement, 3 * (32 - Channels) -
;                            74h. Note 74h where the mono loop uses 70h:
;                            this loop's tail is four bytes longer, so the
;                            same "jump back into the unrolled run" needs a
;                            different bias. Two constants, one for each
;                            loop, and the configurator writes both.
;
;     THERE IS NO SATURATION HERE, and that is the real difference from the
;     mono path rather than the obvious one. AX and BX are not two halves of
;     one signal to be summed -- they ARE the two channels, and the filter
;     pair has already clamped both. So the loop goes straight from the row
;     advance to the output conversion with no ADD and no clamp.
;
;     THE PACK IS THREE INSTRUCTIONS. XOR AH,80h and XOR BH,80h convert both
;     to unsigned, then MOV AL,AH / MOV AH,BH puts the left sample in AL and
;     the right in AH, and one word store writes the pair. Taking the high
;     bytes IS the downshift, as in the mono loop.
;     ==================================================================
@@StereoMix:
    DB 41h                                 ; 0dfc  INC  CX
    JMP     @@StereoPrime                  ; 0dfd  enter at the bottom
    DB 03h, 44h, 3Eh                       ; 0dff  ADD  AX,[SI+3eh] -- ch 31
    DB 03h, 5Ch, 3Ch                       ; 0e02  ADD  BX,[SI+3ch] -- ch 30
    DB 03h, 5Ch, 3Ah                       ; 0e05  ADD  BX,[SI+3ah] -- ch 29
    DB 03h, 44h, 38h                       ; 0e08  ADD  AX,[SI+38h] -- ch 28
    DB 03h, 44h, 36h                       ; 0e0b  ADD  AX,[SI+36h] -- ch 27
    DB 03h, 5Ch, 34h                       ; 0e0e  ADD  BX,[SI+34h] -- ch 26
    DB 03h, 5Ch, 32h                       ; 0e11  ADD  BX,[SI+32h] -- ch 25
    DB 03h, 44h, 30h                       ; 0e14  ADD  AX,[SI+30h] -- ch 24
    DB 03h, 44h, 2Eh                       ; 0e17  ADD  AX,[SI+2eh] -- ch 23
    DB 03h, 5Ch, 2Ch                       ; 0e1a  ADD  BX,[SI+2ch] -- ch 22
    DB 03h, 5Ch, 2Ah                       ; 0e1d  ADD  BX,[SI+2ah] -- ch 21
    DB 03h, 44h, 28h                       ; 0e20  ADD  AX,[SI+28h] -- ch 20
    DB 03h, 44h, 26h                       ; 0e23  ADD  AX,[SI+26h] -- ch 19
    DB 03h, 5Ch, 24h                       ; 0e26  ADD  BX,[SI+24h] -- ch 18
    DB 03h, 5Ch, 22h                       ; 0e29  ADD  BX,[SI+22h] -- ch 17
    DB 03h, 44h, 20h                       ; 0e2c  ADD  AX,[SI+20h] -- ch 16
    DB 03h, 44h, 1Eh                       ; 0e2f  ADD  AX,[SI+1eh] -- ch 15
    DB 03h, 5Ch, 1Ch                       ; 0e32  ADD  BX,[SI+1ch] -- ch 14
    DB 03h, 5Ch, 1Ah                       ; 0e35  ADD  BX,[SI+1ah] -- ch 13
    DB 03h, 44h, 18h                       ; 0e38  ADD  AX,[SI+18h] -- ch 12
    DB 03h, 44h, 16h                       ; 0e3b  ADD  AX,[SI+16h] -- ch 11
    DB 03h, 5Ch, 14h                       ; 0e3e  ADD  BX,[SI+14h] -- ch 10
    DB 03h, 5Ch, 12h                       ; 0e41  ADD  BX,[SI+12h] -- ch 9
    DB 03h, 44h, 10h                       ; 0e44  ADD  AX,[SI+10h] -- ch 8
    DB 03h, 44h, 0Eh                       ; 0e47  ADD  AX,[SI+0eh] -- ch 7
    DB 03h, 5Ch, 0Ch                       ; 0e4a  ADD  BX,[SI+0ch] -- ch 6
    DB 03h, 5Ch, 0Ah                       ; 0e4d  ADD  BX,[SI+0ah] -- ch 5
    DB 03h, 44h, 08h                       ; 0e50  ADD  AX,[SI+08h] -- ch 4
    DB 03h, 44h, 06h                       ; 0e53  ADD  AX,[SI+06h] -- ch 3
    DB 03h, 5Ch, 04h                       ; 0e56  ADD  BX,[SI+04h] -- ch 2
    DB 81h, 0C6h                           ; 0e59  ADD  SI,imm16 ...
@@Stride3:
    DW 1234h                               ; 0e5b  ... the row stride, patched
    DB 80h, 0F4h, 80h                      ; 0e5d  XOR  AH,80h -- left to unsigned
    DB 80h, 0F7h, 80h                      ; 0e60  XOR  BH,80h -- right
    DB 8Ah, 0C4h                           ; 0e63  MOV  AL,AH -- left into the low byte
    DB 8Ah, 0E7h                           ; 0e65  MOV  AH,BH -- right into the high
    DB 26h, 89h, 05h                       ; 0e67  MOV  ES:[DI],AX -- both, one store
    DB 47h                                 ; 0e6a  INC  DI
    DB 47h                                 ; 0e6b  INC  DI
@@StereoPrime:
    DB 8Bh, 04h                            ; 0e6c  MOV  AX,[SI]     -- channel 0
    DB 8Bh, 5Ch, 02h                       ; 0e6e  MOV  BX,[SI+02h] -- channel 1
    DB 0E2h                                ; 0e71  LOOP ...
@@StereoLoop:
    DB 8Ch                                 ; 0e72  ... displacement patched, bias 74h
    DB 0C3h                                ; 0e73  RET -- NEAR

;  ONE BYTE OF STATE, IN THE CODE SEGMENT AGAIN. Written by
;
@@Flag0e74:
    DB 0

;  ==================================================================
;     THE BUFFER-FILL DRIVER. Frameless, near. Configures the
;
;     IT REPURPOSES DS, AND THAT IS WHY THE SS: OVERRIDES ARE THERE.
;     `LDS SI,Sounding` at 0e97 points DS at the SAMPLE DATA so the output
;     loops can walk it with plain [SI] addressing -- which costs nothing in
;     the inner loop and is the whole point. But DGROUP is then unreachable
;     through DS, so every global touched after that line is read through
;     SS: instead. SS and DS are the same SEGMENT in a Pascal program, so
;     the override is free. Miss one and it reads the sample buffer as if it
;     were the data SEGMENT.
;
;     THE WRAP IS HANDLED AS TWO CALLS, NOT A MODULO. The buffer is
;     circular; if this batch would run past the end, the first call writes
;     as many whole samples as fit (0eb6 divides the remaining bytes by
;     BytesPerSample to get that count), then DI is reset to the start and
;     the second call writes the rest. Where it fits, 0ea4 jumps straight to
;     the single call.
;
;     Note the JC at 0e9e: the end-of-write address is computed as AX+DI and
;     can carry out of 16 bits, which is a wrap too -- tested before the
;     ordinary CMP, because CMP would compare a wrapped value and get it
;     wrong.
;
;     0eda IS WHERE ActualBuffer^.InUse IS CLEARED, and it answers a
;     question left open by DoGetBuffer: 1.39b clears the flag when it
;     RETIRES a block, 1.31's DoGetBuffer does not, and it is done here
;     instead -- when the mixer has actually finished reading the block
;     rather than when the queue moves on. Not the leak it looked like.
;     ==================================================================
@@FillBuffer:
    DB 32h, 0C0h                           ; 0e75  XOR  AL,AL
    DB 2Eh, 0A2h                           ; 0e77  MOV  CS:[@@Flag0e74],AL -- clear it
    DW OFFSET @@Flag0e74
    CALL    @@Configure                    ; 0e7b  the kernels and the gains

    MOV     CX,SoundLeft                   ; 0e7e  how many samples are available
    MOV     AL,BytesPerSample
    DB 8Bh, 0D1h                           ; 0e85  MOV  DX,CX
    DB 32h, 0E4h                           ; 0e87  XOR  AH,AH
    DB 0F7h, 0E2h                          ; 0e89  MUL  DX -- bytes, not samples

    LES     DI,DMABufferPtr                ; 0e8b  where the mixer got to
    MOV     BX,WORD PTR DMABuffer          ; 0e8f  the buffer's offset
    ADD     BX,DMABufferSize               ; 0e93  ... so BX is one past its end
    LDS     SI,Sounding                    ; 0e97  DS NOW POINTS AT THE SAMPLES
    DB 0FCh                                ; 0e9b  CLD

    DB 03h, 0C7h                           ; 0e9c  ADD  AX,DI -- where writing ends
    JC      @@Split                        ; 0e9e  carried out of 16 bits: also a wrap
    DB 90h                                 ; 0ea0  NOP -- jump-sizing padding
    DB 90h                                 ; 0ea1  NOP
    DB 3Bh, 0C3h                           ; 0ea2  CMP  AX,BX
    JBE     @@OneGo                        ; 0ea4  it fits -- one call and done
@@Split:
    DB 2Bh, 0DFh                           ; 0ea6  SUB  BX,DI -- bytes to the end
    JZ      @@Wrap                         ; 0ea8  none, so start at the beginning
    DB 51h                                 ; 0eaa  PUSH CX
    DB 8Bh, 0C3h                           ; 0eab  MOV  AX,BX
    DB 36h, 8Ah, 0Eh                       ; 0ead  MOV  CL,SS:[BytesPerSample] -- SS:
    DW OFFSET BytesPerSample
    DB 32h, 0EDh                           ; 0eb2  XOR  CH,CH
    DB 33h, 0D2h                           ; 0eb4  XOR  DX,DX
    DB 0F7h, 0F1h                          ; 0eb6  DIV  CX -- whole samples that fit
    DB 8Bh, 0C8h                           ; 0eb8  MOV  CX,AX
    DB 51h                                 ; 0eba  PUSH CX
    DB 36h, 0FFh, 16h                      ; 0ebb  CALL WORD PTR SS:[MixRut]
    DW OFFSET MixRut
    DB 5Bh                                 ; 0ec0  POP  BX
    DB 59h                                 ; 0ec1  POP  CX
    DB 2Bh, 0CBh                           ; 0ec2  SUB  CX,BX -- what is left over
@@Wrap:
    DB 36h, 8Bh, 3Eh                       ; 0ec4  MOV  DI,SS:[DMABuffer] -- rewind
    DW OFFSET DMABuffer
@@OneGo:
    DB 36h, 0FFh, 16h                      ; 0ec9  CALL WORD PTR SS:[MixRut]
    DW OFFSET MixRut

    DB 8Ch, 0D0h                           ; 0ece  MOV  AX,SS
    DB 8Eh, 0D8h                           ; 0ed0  MOV  DS,AX -- DGROUP back
    MOV     WORD PTR DMABufferPtr,DI
; 0ed2  remember where we got to -- the OFFSET
; half only, which is what 89 3E stores
    LES     DI,ActualBuffer
    DB 26h, 0C6h, 05h, 00h                 ; 0eda  MOV  BYTE PTR ES:[DI],0 -- InUse
; := FALSE; see the note above
    DB 0C3h                                ; 0ede  RET -- NEAR

;  ==================================================================
;     @@Prepare. What SharedPoll calls before the installed
;
;     THE IDLE COUNTER AT 0f32 IS A GIVING-UP RULE, not a timer: the flag at
;     @@Flag0e74 is incremented each time there is nothing to do, and once it
;     reaches 20 the increment stops and DeviceIdling is set from it. So
;     "idle" means twenty consecutive polls with no work, not a duration.
;     ==================================================================
@@Prepare:
    MOV     AL,IsDmaDevice
    DB 22h, 0C0h                           ; 0ee2  AND  AL,AL
    JNZ     @@PrepFetch                    ; 0ee4  the DMA path is much shorter
    DB 90h                                 ; 0ee6  NOP -- jump-sizing padding
    DB 90h                                 ; 0ee7  NOP
    MOV     AX,SoundLeft
    MOV     BL,DMABufferYet
    DB 22h, 0DBh                           ; 0eef  AND  BL,BL
    JZ      @@PrepMark
    DB 0Eh                                 ; 0ef3  PUSH CS -- the same-segment far call
    CALL    NEAR PTR DoGetBuffer
    DB 90h                                 ; 0ef7  NOP
@@PrepMark:
    DB 0B3h, 01h                           ; 0ef8  MOV  BL,1
    MOV     DMABufferYet,BL

    DB 3Dh, 0Ah, 00h                       ; 0efe  CMP  AX,10 -- fewer than ten samples
    JC      @@PrepIdle                     ; 0f01  ... is not worth mixing for
    MOV     DL,BytesPerSample
    DB 32h, 0F6h                           ; 0f07  XOR  DH,DH
    DB 0F7h, 0E2h                          ; 0f09  MUL  DX -- samples to bytes
    DB 50h                                 ; 0f0b  PUSH AX
    CALL    GetDMACount                    ; 0f0c  where the card has drained to
    MOV     BX,DMABufferSize
    DB 2Bh, 0D8h                           ; 0f13  SUB  BX,AX -- bytes already played
    ADD     BX,WORD PTR DMABuffer          ; 0f15  ... as an offset
    DB 8Bh, 0CBh                           ; 0f19  MOV  CX,BX
    SUB     BX,WORD PTR DMABufferPtr       ; 0f1b  how far ahead the mixer is
    DB 73h, 04h                            ; 0f1f  JNC -- no wrap
    ADD     BX,DMABufferSize               ; 0f21  wrapped, so add a lap
    DB 58h                                 ; 0f25  POP  AX
    DB 2Bh, 0D8h                           ; 0f26  SUB  BX,AX
    DB 0Fh, 83h, 49h, 0FFh                 ; 0f28  JNB @@FillBuffer -- a 386 NEAR
; conditional, out of short range. Its
; displacement is self-relative and both
; ends are in this run, so it is a literal
    DB 32h, 0DBh                           ; 0f2c  XOR  BL,BL
    MOV     DMABufferYet,BL
@@PrepIdle:
    DB 2Eh, 0A0h                           ; 0f32  MOV  AL,CS:[@@Flag0e74]
    DW OFFSET @@Flag0e74
    DB 3Ch, 14h                            ; 0f36  CMP  AL,20
    JNC     @@PrepDone                     ; 0f38  already given up
    DB 90h                                 ; 0f3a  NOP
    DB 90h                                 ; 0f3b  NOP
    DB 2Eh, 0FEh, 06h                      ; 0f3c  INC  BYTE PTR CS:[@@Flag0e74]
    DW OFFSET @@Flag0e74
@@PrepDone:
    MOV     DeviceIdling,AL
    DB 0C3h                                ; 0f44  RET -- NEAR
@@PrepFetch:
    DB 0Eh                                 ; 0f45  PUSH CS
    CALL    NEAR PTR DoGetBuffer
    DB 90h                                 ; 0f49  NOP
    LES     DI,ActualBuffer
    DB 26h, 0C6h, 05h, 00h                 ; 0f4e  MOV  BYTE PTR ES:[DI],0 -- InUse
    DB 0C3h                                ; 0f52  RET -- NEAR

;  ==================================================================
;     THE INT 8 HANDLER. Its middle is a CHAIN THROUGH THE
;
;         0f53  entry, save, set DS   JMP [0bf8] -> 0f64
;         0f64  one sample left?      JMP [0bfa] -> 104d
;         104d  wait, DSP command     JMP           0f73
;         0f73  mix one sample        JMP [0bfc] -> 1063
;         1063  wait, write the byte  JMP           0f8d
;         0f8d  the tail below
;
;     Every step is a JMP, never a CALL, so there is no return path to
;     unwind -- the chain simply ends at the tail, which does the periodic
;     bookkeeping and IRETs.
;     ==================================================================
@@Int8:
    DB 0FAh                                ; 0f53  CLI
    DB 50h                                 ; 0f54  PUSH AX
    DB 53h                                 ; 0f55  PUSH BX
    DB 51h                                 ; 0f56  PUSH CX
    DB 52h                                 ; 0f57  PUSH DX
    DB 57h                                 ; 0f58  PUSH DI
    DB 56h                                 ; 0f59  PUSH SI
    DB 1Eh                                 ; 0f5a  PUSH DS
    MOV     AX,SEG @DATA                   ; 0f5b  the original: MOV AX,1caa
    DB 8Eh, 0D8h                           ; 0f5e  MOV  DS,AX
    DB 0FFh, 26h                           ; 0f60  JMP  WORD PTR [OutSlot0]
    DW OFFSET OutSlot0
@@Prim0:
    MOV     AX,SoundLeft
    DB 23h, 0C0h                           ; 0f67  AND  AX,AX
    JZ      @@TimerTail                    ; 0f69  nothing to play
    DB 48h                                 ; 0f6b  DEC  AX
    MOV     SoundLeft,AX
    DB 0FFh, 26h                           ; 0f6f  JMP  WORD PTR [OutSlot1]
    DW OFFSET OutSlot1
@@Prim2:
    DB 32h, 0FFh                           ; 0f73  XOR  BH,BH
    MOV     DeviceIdling,BH                ; 0f75  we are doing work
    LDS     SI,Sounding                    ; 0f79  DS to the samples, as in the filler
    CALL    @@DownMix                      ; 0f7d  ONE sample through the kernel
    MOV     DX,SEG @DATA
    DB 8Eh, 0DAh                           ; 0f83  MOV  DS,DX -- DGROUP back
    MOV     WORD PTR Sounding,SI           ; 0f85  remember the read position
    DB 0FFh, 26h                           ; 0f89  JMP  WORD PTR [OutSlot2]
    DW OFFSET OutSlot2

; The tail. Reached from 1063, and the only path that returns.
@@TimerTail:
    DB 0FBh                                ; 0f8d  STI
    MOV     AX,SystemClockIncr
    ADD     ClockAccum,AX                  ; 0f91  accumulate towards 18.2 Hz
    JC      @@ChainOld                     ; 0f95  a whole BIOS tick has passed
    DB 0B0h, 20h                           ; 0f97  MOV  AL,20h
    DB 0E6h, 20h                           ; 0f99  OUT  20h,AL -- EOI
@@AfterEoi:
    MOV     CX,PeriodicCount
    DB 0E3h, 1Dh                           ; 0f9f  JCXZ @@Periodic
    DB 49h                                 ; 0fa1  DEC  CX
    MOV     PeriodicCount,CX
    JZ      @@Periodic                     ; 0fa6  it fired
    MOV     AX,SoundLeft
    DB 23h, 0C0h                           ; 0fab  AND  AX,AX
    JZ      @@Periodic                     ; 0fad  out of samples -- go and get some
@@Iret:
    DB 1Fh                                 ; 0faf  POP  DS
    DB 5Eh                                 ; 0fb0  POP  SI
    DB 5Fh                                 ; 0fb1  POP  DI
    DB 5Ah                                 ; 0fb2  POP  DX
    DB 59h                                 ; 0fb3  POP  CX
    DB 5Bh                                 ; 0fb4  POP  BX
    DB 58h                                 ; 0fb5  POP  AX
    DB 0CFh                                ; 0fb6  IRET
@@ChainOld:
    DB 9Ch                                 ; 0fb7  PUSHF -- the old handler expects it
    DB 0FFh, 1Eh                           ; 0fb8  CALLF [OldTimerHandler]
    DW OFFSET OldTimerHandler
    JMP     @@AfterEoi                     ; 0fbc  PAST our EOI, not to it: the BIOS
; handler we just called has already
; acknowledged the interrupt, and a second
; OUT 20h,AL would end an interrupt that
; is not in service

;  ==================================================================
;     0fbe -- the periodic callback, behind a guard that lives IN THE CODE.
;
;     @@Guard0fbf is the IMMEDIATE of the MOV AL at 0fbe, and 0fc6 writes
;     over it. The fourth place this unit keeps state inside the instruction
;     stream, after the poll's guard at CS:$104b, the filter chains' four
;     sample immediates, and @@Flag0e74.
;
;     It has to be a guard: the callback is a music interpreter that can run
;     for a while, and the timer will fire again inside it.
;     ==================================================================
@@Periodic:
    DB 0B0h                                ; 0fbe  MOV  AL,imm8 ...
@@Guard0fbf:
    DB 0                                   ; 0fbf  ... and the immediate IS the guard
    DB 22h, 0C0h                           ; 0fc0  AND  AL,AL
    JNZ     @@Iret                         ; 0fc2  already inside -- just return
    DB 0FEh, 0C0h                          ; 0fc4  INC  AL
    DB 2Eh, 0A2h                           ; 0fc6  MOV  CS:[@@Guard0fbf],AL -- take it
    DW OFFSET @@Guard0fbf

    MOV     AX,SoundLeft
    DB 23h, 0C0h                           ; 0fcd  AND  AX,AX
    JNZ     @@Release                      ; 0fcf  there is data -- nothing to fetch
    CALL    @@Configure
    DB 06h                                 ; 0fd4  PUSH ES
    DB 0Eh                                 ; 0fd5  PUSH CS
    CALL    NEAR PTR DoGetBuffer
    DB 90h                                 ; 0fd9  NOP
    DB 07h                                 ; 0fda  POP  ES
    CALL    @@Configure                    ; 0fdb  again: DoGetBuffer may have changed
; the rate, and the kernels encode it
    MOV     AX,SoundLeft
    DB 23h, 0C0h                           ; 0fe1  AND  AX,AX
    JNZ     @@Release
    DB 0FEh, 0C4h                          ; 0fe5  INC  AH
    MOV     DeviceIdling,AH                ; 0fe7  still nothing: idle
@@Release:
    DB 2Eh, 0C6h, 06h                      ; 0feb  MOV  BYTE PTR CS:[@@Guard0fbf],0
    DW OFFSET @@Guard0fbf
    DB 00h

    MOV     AX,PeriodicCount
    DB 23h, 0C0h                           ; 0ff4  AND  AX,AX
    JNZ     @@Done                         ; 0ff6  not due yet
    MOV     AX,PeriodicStart
    MOV     PeriodicCount,AX               ; 0ffb  reload the countdown
    DB 06h                                 ; 0ffe  PUSH ES
    DB 0FBh                                ; 0fff  STI -- the callback may take a while
    DB 0FFh, 1Eh                           ; 1000  CALLF [PeriodicProc]
    DW OFFSET PeriodicProc
    DB 07h                                 ; 1004  POP  ES
@@Done:
    JMP     @@Iret

;  1007 -- one byte of padding in the original, and the ONE byte of this
;     whole run that does not match. The run ends on an unconditional JMP and
;     the original needs no return after it; Pascal emits a near RET there
;     regardless -- confirmed in the probe, which shows the epilogue appearing
;     after a trailing internal JMP, after a written RET and after pure data
;     alike, so no arrangement of an `assembler` procedure suppresses it.
;
;     The three bytes at 0746 that used to be the other half of this note are
;     GONE: nothing calls SetGains by name, so the procedure never needed a
;     reachable first instruction. This byte is what is left, and it is not a
;     Pascal limitation either -- the original's 00 is the padding an external
;     TASM module leaves when it aligns the next PUBLIC label. See the encoding
;     table at the head of SOUNDDEV.PAS. One byte in 2,242.

